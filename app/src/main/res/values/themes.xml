<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.HabitHive" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.HabitHive" parent="Base.Theme.HabitHive" />

    <!-- styles.xml or themes.xml -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Other theme attributes -->
        <item name="popupTheme">@style/AppTheme.PopupOverlay</item>
    </style>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.MaterialComponents.Light">
        <!-- Define styles for popups and popup menus here -->
        <item name="android:colorBackground">@color/colorUnchecked</item>
        <item name="android:textColor">@color/primaryTextColor</item>
        <!-- Add more styles as needed -->
    </style>

</resources>
